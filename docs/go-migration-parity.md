# Go migration parity matrix

This document tracks the side-by-side Go rewrite. The Python harness remains the
behavioral reference and the operational entry point until a separately approved
cutover. `Complete` means the listed behavior has direct Go tests or conformance
evidence; it does not mean that a larger calling flow is complete.

Status values are `Not started`, `Partial`, and `Complete`.

| Behavior / Python owner | Go owner | Compatibility requirements | Python evidence | Go evidence | Status / differences |
|---|---|---|---|---|---|
| Frame codec (`framing.py`) | `internal/guest` | Exact little-endian header; 16 MiB bound; fragmentation, coalescing, invalid-prefix, and closure behavior | `test_framing.py` | `framing_test.go`, `conformance_test.go`, framing fuzz target | Complete for codec; transport deadlines remain with the serial dispatcher |
| Source snapshot codec (`source_snapshot.py`) | `internal/guest` | Exact encoding; bounds; UTF-8; duplicate and `seed/` path rejection; canonical round trips | `test_source_snapshot.py`, guest build/finish tests | `snapshot_test.go`, `conformance_test.go`, snapshot fuzz target | Complete for codec |
| Host-service request/response codec (`host_service_protocol.py`) | `internal/guest` | Exact message IDs, nonzero request IDs, limits, binary arguments/status/output | `test_host_service_protocol.py` | `protocol_test.go` | Complete for codec |
| Guest tool codec/client (`tool_protocol.py`) | `internal/guest` | Discovery/invocation encodings, request-ID rollover and matching, five-second exchange timeout | `test_tool_protocol.py` | `protocol_test.go`, `tool_client_test.go` | Complete |
| Serial connection and READY (`serial.py`, `guest_startup.py`) | `internal/guest` | Unix connection deadlines, bounded diagnostics, canonical READY marker | `test_serial.py`, `test_candidate_boot.py`, `test_seed_boot.py` | `serial_test.go`, `dispatcher_test.go`, `candidate_test.go` | Complete for the transport, READY protocol, and disposable candidate wiring |
| Duplex serial dispatcher (`serial_protocol.py`) | `internal/guest` | One reader; dispatcher-owned ordered writes; bounded queue/write/shutdown; nested host calls; timeout suspension and progress deadline | `test_tool_protocol.py`, `test_guest_build.py`, `test_guest_finish_generation.py`, `test_guest_feature_requests.py` | `dispatcher_test.go`, `dispatcher_stress_test.go` | Complete; unlike Python's unbounded deque, Go fails closed above 8 queued writes or approximately 32 MiB |
| Build and finish host services (`build_host_service.py`, `generation_finish_host_service.py`) | `internal/build`, `internal/guest`, `internal/experiment` | Fixed build operation only; staged validated candidate; selected-successor freeze; feature/assets routing | `test_build_host_service.py`, guest build/finish/feature tests | `host_service_test.go`, trusted-build and synthetic candidate integration, `live_test.go` | Complete: the live dispatcher routes the joined host services and an accepted finish closes the guest, revalidates candidate identities while streaming the immutable archive, and freezes the selected successor at the gate |
| Trusted compilation (`trusted_build.py`) | `internal/build` | Input validation, deterministic materialization, fixed sandbox command, ISO construction, bounded diagnostics and process cleanup | `test_trusted_build.py`, `test_guest_build.py` | `trusted_test.go`, embedded-source Python conformance, disposable Python trusted-build integration | Complete for the fixed compilation operation and guest-facing build-service integration |
| Candidate boot validation (`candidate_boot.py`) | `internal/build` | Exact hardware profile, READY and tool-protocol proof, cleanup, fail-closed status | `test_candidate_boot.py` | `candidate_test.go`, synthetic paused-QEMU/QMP/serial integration, `host_service_test.go` | Complete for isolated proof and build-service integration, including fail-closed forensic provenance |
| Feature ledger (`feature_requests.py`) | `internal/store`, `internal/experiment` | Sparse monotonic IDs, UTF-8 byte bounds, atomic durable decisions, gate-only approval/denial | `test_feature_requests.py`, feature guest/console tests | `feature_requests_test.go`, `feature_requests_conformance_test.go`, persistent-record fuzz target, `live_test.go` | Complete for uint64 IDs/generations, including guest recording and gate-only runtime decisions; Python's larger-than-uint64 integers are conservatively rejected |
| Provided assets (`provided_assets.py`) | `internal/store`, `internal/guest`, `internal/experiment` | Deterministic frozen bytes/tree tar, append-only manifest revisions, path/content privacy, bounded reads | `test_provided_assets.py` | `provided_assets_test.go`, `provided_assets_conformance_test.go`, live runtime and runner integration | Complete for snapshot, manifest, operator flag routing, startup/background/exchange routing, and gate-reopen restoration |
| Build/review forensic evidence (`forensic_provenance.py`) | `internal/provenance` | Immutable allocation, hashes, incomplete markers, atomic manifests, latest-success fail-closed behavior | `test_forensic_provenance.py` | `forensic_test.go`, `forensic_conformance_test.go`, `reviewer_test.go`, `host_service_test.go`, live runtime integration | Complete for evidence capture and reviewer/build publication through the live generation session |
| Planning evidence (`planning_evidence.py`) | `internal/provenance` | Private text, public digest/identity only, attempts and interruption/failure durability | `test_planning_evidence.py`, generation worker tests | `planning_test.go`, `planning_conformance_test.go`, `generation_test.go`, operator session integration | Complete for byte-compatible recording, interruption, continuation, failure, fail-closed publication, and live operator ownership |
| Exit-interview transcript (`exit_interview_transcript.py`) | `internal/provenance` | Immutable finalized artifact, partial turns, safe paths, no prompt/handoff reuse | `test_exit_interview_transcript.py`, console/worker tests | `exit_interview_test.go`, `exit_interview_conformance_test.go`, `generation_test.go`, `console_test.go` | Complete for capture, rendering, immutable operator finalization, retained same-thread turns, tool denial, interruption, shutdown, and partial-close preservation |
| Cross-run bootstrap (`cross_run_bootstrap.py`) | `internal/store`, `internal/experiment` | Immutable source identity, ISO/hash/Git verification, collision and mismatch failure, inherited handoff/ledger | `test_cross_run_bootstrap.py`, `test_gate_reopen.py` | `cross_run_bootstrap_conformance_test.go`, Python read-back, `live_test.go` | Complete for initialization, loading, initial-ISO verification, inherited handoff, and feature-ledger use by the live runtime |
| Run/generation lifecycle (`generation_runtime.py`) | `internal/experiment` | States and gates, archive format, continue/fork/abort/pause/resume/stop, rollback ancestry, compatible reopen | `test_generation_runtime.py`, `test_gate_reopen.py`, guest integration tests | `runtime_test.go`, `live_test.go`, `generation_test.go`, `console_test.go`, bidirectional Python archive conformance | Complete for concrete QEMU/QMP/serial/tool/host-service ownership, streamed completion and abort, pause/resume, boot-first continue/rollback, gate reopen, cancellation, gate leasing, and fresh-session operator orchestration |
| Hardware manifest/profile (`hardware.py`) | `internal/qemu` | Exact q35 device/profile arguments, QEMU version, canonical manifest validation | `test_hardware.py` | `hardware_test.go`, `hardware_conformance_test.go`, manifest fuzz target | Complete for profiles, command construction, version discovery, and manifest codec |
| QEMU/QMP ownership (`qemu.py`, `qmp.py`) | `internal/qemu` | Direct-child lifecycle, bounded TERM/KILL, log ownership, QMP greeting/capabilities/commands/errors | `test_qemu.py`, `test_qmp.py` | `process_test.go`, `qmp_test.go`, disposable combined QEMU/QMP integration | Complete; Go rejects QMP messages over 1 MiB and reaps exited children asynchronously |
| Generation Git lineage (`generation_git.py`) | `internal/provenance` | Base-ref identity, commit/tree/tag messages and namespace, reconciliation without worktree mutation, immutable conflicts | `test_generation_git.py`, `test_gate_reopen.py` | `generation_git_test.go`, `console_test.go`, runner startup validation, Python object/message conformance | Complete for archive reconciliation, immutable refs, operator commands, startup base verification, and cross-run provenance |
| App-server transport (`codex_app_server.py`) | `internal/codexapp` | JSONL process protocol, catalog validation, notifications, interrupts, errors, bounded stderr and shutdown | generation/review worker tests, fake app server | codec/token tests, `app_server_test.go`, generation/reviewer synthetic app-server integration, operator session tests, fuzz, and Python conformance | Complete for subprocess ownership and live operator use by review, planning, implementation, and interview sessions |
| Implementor/planner session (`codex_generation_worker.py`) | `internal/agent` | Same thread for planning and implementation, exact policies/prompts, dynamic tools, pause/resume, token accounting | `test_codex_generation_worker.py` | `generation_test.go`, `console_test.go`, synthetic app-server integration | Complete for same-thread planning/implementation, exact serving policy and prompt context, dynamic tools, planning interruption/resumption, pause/resume, retained tool-less exit interviews, review calls, evidence, activity, token accounting, fresh-generation ownership, and bounded cancellation |
| Reviewer (`codex_review_worker.py`) | `internal/agent` | Separate read-only session, source evidence, fixed model/policy, cancellation and result isolation | `test_codex_review_worker.py` | `reviewer_test.go`, `generation_test.go`, synthetic app-server integration | Complete: each consultation owns a fresh process, workspace, and thread and is invoked through the live generation session's review tool |
| Activity and observability (`codex_activity.py`, `observability.py`) | `internal/observability` | Structured event schema, sequence/recovery, bounded labels, private text exclusion, optional OTLP | activity/observability/forensic tests | `activity_test.go`, `events_test.go`, `metrics_test.go`, runner tests, local OTLP integration | Complete for durable events, live activity, exact metric instruments, bounded labels, optional OTLP/HTTP export, and live lifecycle/operator producers |
| Operator commands/CLI (`operator_console.py`) | `internal/operator`, `cmd/codexos` | Existing flags/defaults/validation/exit codes; command confirmations; non-TUI behavior; clean session/runtime ordering | `test_operator_console.py`, `test_gate_reopen.py`, `test_provided_assets.py` | `cli_test.go`, `console_test.go`, `run_test.go`, `terminal_test.go`, `text_test.go`, `cmd/codexos/main_test.go`, standalone-QEMU live-runner acceptance, real-binary gate acceptance | Complete for Cobra validation and exit classification, explicit flag presence, bootstrap/Git validation, concrete runtime startup, all line-console commands, session ownership, exit interviews, shutdown ordering, and the side-by-side Go process entry point; Python remains the default operational entry point |
| TUI model and application (`operator_tui_model.py`, `operator_tui.py`) | `internal/tui` | Bubble Tea v2 cursed renderer; attribution/transcript/selection/prompts; shutdown and terminal restoration | `test_operator_tui_model.py`, `test_operator_tui.py` | `model_test.go`, `application_test.go`, `application_pty_test.go`, concrete operator adapter, real-binary PTY gate acceptance | Complete: the bounded Bubble Tea v2 cursed application is wired to the authoritative operator console/runtime with one shared session owner, asynchronous output, confirmations, status, finalized-message Markdown, bounded command joining, and race-tested graceful terminal restoration for owned shutdown paths; the full disposable live interactive scenario and Bubble Tea's residual internal-error risk remain cutover concerns |

## Operational entry points and formats

The current Python entry point is `python -m harness.operator_console`. Its
required opening mode is exactly one of `--initial-iso` and `--resume-at-gate`.
The remaining public flags are `--run-directory`, `--git-repository`,
`--git-base-ref`, `--inherit-from-run`, `--inherit-from-generation`,
`--provided-assets`, `--otlp-endpoint`, `--plain`, and `--tui`. Pairing and mode
validation in `operator_console.main` is part of parity.

Authoritative persistent formats include generation directories and metadata,
selected source/ISO and handoff, aborted archives, hardware manifests, events
JSONL, feature request records, provided-assets manifests, build/review/planning
evidence, cross-run bootstrap records, Git refs/commits/tags, and finalized exit
interview Markdown. Their schemas and durability behavior must be derived from
the Python readers/writers and their failure-path tests before Go publishes any
of them.
